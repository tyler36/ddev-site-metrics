##ddev-generated
apiVersion: 1

datasources:
- name: Tempo
  uid: tempo
  type: tempo
  access: proxy
  url: http://grafana-tempo:3200
  basicAuth: false
  jsonData:
    httpMethod: GET
    tracesToMetrics:
      datasourceUid: 'prometheus'
    serviceMap:
      enabled: true
      datasourceUid: 'prometheus'
    tracesToLogsV2:
      datasourceUid: 'loki'
    nodeGraph:
      enabled: true
    traceQuery:
      timeShiftEnabled: true
    spanBar:
      type: 'Tag'
      tag: 'http.path'
